/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.pravatar.cc',
      'source.unsplash.com',
      'images.unsplash.com',
      'placehold.co',
      'res.cloudinary.com',
    ],
  },
};

export default nextConfig;
